services:
  tele-jam-api:
    build: .
    image: tele-jam-api
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
    restart: unless-stopped
    ports:
      - "5002:5002"
      # environment:
      #   - ENV_VAR=your_value